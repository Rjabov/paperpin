# Copy to .env (gitignored). Keys are read from the environment only when a
# model call is explicitly requested — nothing else ever leaves the machine.
GEMINI_API_KEY=
# OPENAI_API_KEY=
# OPENAI_BASE_URL=
